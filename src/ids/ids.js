const ids = {
    Url: String(import.meta.env.VITE_URL),
    PId: String(import.meta.env.VITE_PID),
    DId: String(import.meta.env.VITE_DID),
    CId: String(import.meta.env.VITE_CID),
    K1: String(import.meta.env.VITE_K1),
}

export default ids